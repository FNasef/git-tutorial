#include <stdio.h>
int main() {
    int n;
    char answer, y;
    do {
    printf("please, enter a Number! \n");
    scanf("%d", &n);
    char* num_to_words(int n);
    printf("Do you want to use it again ? (y/n)\n");
    scanf("%s",&answer);
    }
    while (answer=='y');
    return 0;
}